export type request = {
    url: string
}

export type response = {
    
        URL             : string,    
        CustormShort    : number,
        Expiry          : number,
        XRateRemaining  : number,
        XRateLimitReset : number
    
}